# GitHub Pages + Apps Script 安全同步說明

## 建議架構
現在建議統一改成這個模式：

1. `GitHub Pages` 放前端網頁
2. `Apps Script Web App` 當加密同步橋接
3. `Google 試算表` 當雲端資料庫

這樣的好處是：

- 前端原始碼可以公開放 GitHub
- 考生個資不會直接寫死在 GitHub
- 書記進頁面後，要先輸入保護密碼才會載入名單
- Google 試算表會保留 `科系_學生名單`、`科系_自動排程`、`科系_設定`
- 學生資料會先在瀏覽器端加密，再送到試算表

## 你現在要用哪一份 Apps Script
如果你要走 GitHub Pages 版本，最簡單的是直接使用這一份：

- [github-apps-script-bridge.gs](/Users/james/Documents/4p/github-apps-script-bridge.gs)

或直接使用原始維護檔：

- [Code.gs](/Users/james/Documents/4p/gas-webapp/Code.gs)

這份現在已經支援：

- GitHub 前端橋接
- 保護密碼驗證
- 加密學生資料儲存
- 遮罩欄位寫入試算表
- 每科系三張工作表

不需要再依賴內嵌的 `Index.html` 才能當後端。

## 最簡單部署步驟
1. 建立或打開你的 Google 試算表。
2. 點 `擴充功能 > Apps Script`。
3. 把原本 `Code.gs` 內容全部刪掉。
4. 打開 [github-apps-script-bridge.gs](/Users/james/Documents/4p/github-apps-script-bridge.gs)（或 [Code.gs](/Users/james/Documents/4p/gas-webapp/Code.gs)），整份全選複製，貼進 Apps Script 的 `Code.gs`。
5. 按儲存。
6. 點右上角 `部署 > 新增部署作業 > 網頁應用程式`。
7. 建議設定：
   - `執行身分`: 我
   - `誰可以存取`: 任何知道連結的人
8. 完成授權。
9. 複製部署後的 Web App 網址。

## GitHub Pages 端怎麼接
部署好 GitHub Pages 後，正常情況下我已經把同步網址預先寫進前端，所以書記看到的流程可以簡化成：

1. 打開你的 GitHub Pages 網頁
2. 點 `書記解鎖`
3. 輸入一組至少 8 碼的解鎖密碼
4. 按 `解鎖開始使用`
5. 若是固定辦公室電腦，可勾選 `記住這台電腦 30 天`

也就是說，書記平常不需要再貼 Web App 網址。

只有在下面兩種情況，才需要重新處理網址：

- 你重新部署 Apps Script，產生了新的 Web App 網址
- 你想改接到另一份試算表

第一次連線時：

- 如果 Apps Script 端還沒有資料，系統會問你要不要把目前工作區上傳成第一份雲端資料
- 之後書記只要用同一組保護密碼就能解鎖

## 試算表內會長什麼樣
### `科系_設定`
這張會保留：

- 科系名稱、副標題、圖示
- 時段設定
- 筆試與口試參數
- 一梯幾隊、一隊幾組
- 排法與同校分組偏好

這些不是敏感個資，所以會保留為可讀內容。

### `科系_學生名單`
這張會保留：

- 遮罩後姓名
- 遮罩後生日
- 遮罩後學校
- 偏好與分配時段摘要
- 筆試 / 口試摘要
- 加密後的完整學生資料

也就是說：

- 書記在前端解鎖後，看得到完整資料
- 試算表本身不直接裸露完整個資

### `科系_自動排程`
這張會保留：

- 遮罩學生識別
- 已分配時段
- 筆試 / 口試教室與時間
- 組別摘要
- 狀態備註

方便書記用試算表快速檢視排程狀況，但不直接露出完整名單。

## 個資保護設計
這次保護方式是：

1. GitHub 前端不存密碼在原始碼裡
2. 保護密碼平常只存在目前瀏覽器工作階段
3. 未解鎖前不自動載入完整考生名單
4. 同步前先在瀏覽器端加密
5. 試算表只存遮罩欄位與加密 payload

如果你啟用 `記住這台電腦 30 天`：

- 只建議用在固定辦公室電腦
- 不建議用在共用、借用或不信任的裝置

## 自動備份
Apps Script 仍會保留：

1. 每次同步後寫入 `_系統備份`
2. 每日凌晨 3 點自動建立備份快照

備份內容也會跟著保存加密後資料，而不是明文個資。

## 注意事項
- 如果你還在用舊的 Apps Script 內嵌版頁面，建議改成 GitHub Pages 作為正式入口。
- 若你重新部署 Apps Script，記得把新的 Web App 網址重新貼進 GitHub 前端的 `GitHub 雲端保護` 視窗。
- 保護密碼忘記了，前端就無法解密原本資料，所以請務必由承辦單位妥善保存。
- 若你只是本機預覽 `index.html`，仍可當純本機版使用；只有設定了 GitHub 雲端保護後，才會進入加密同步模式。
